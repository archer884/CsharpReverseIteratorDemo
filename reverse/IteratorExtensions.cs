using System;
using System.Collections.Generic;
using System.Linq;

namespace Reverse
{
    public static class IteratorExtensions
    {
        /// <summary>
        /// Reverses an IEnumerable(T), returning the same values in reverse order
        /// as IEnumerable(T).
        /// </summary>
        public static IEnumerable<T> ReverseSeq<T>(this IEnumerable<T> collection)
        {
            throw new NotImplementedException();
        }
    }
}
